import Box from 'components/abstract/Box';
import theme from 'style/theme';

const TracksZone = () => {
  return <Box w='100%' h='100%' bgc={theme.colors.background1} />;
};

export default TracksZone;
